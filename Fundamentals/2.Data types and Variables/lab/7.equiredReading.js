function reading(pagesOfBook, pagesPerHour, daysForRead) {
    let timeToRead = pagesOfBook / pagesPerHour;
    let hoursPerDay = timeToRead / daysForRead
    console.log(hoursPerDay);
}
reading(432,
    15,
    4

)
