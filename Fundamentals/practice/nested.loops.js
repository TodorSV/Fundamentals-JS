//for (let i = 1; i <= 5; i++) {
for (let j = 1; j <= 10; j++) {
    console.log(`${5} x ${j} = ${5 * j}`);

}
//    console.log('---------------');

//console.log(56 % 2 == 0 ? true : false);

let num = [5, 1, 4, 3, 2, 1]
let top = []
for (let i = 0; i < num.length; i++) {
    let curr = num[i];
    if (Math.max(curr)) {
        top.push(curr)
    }
}